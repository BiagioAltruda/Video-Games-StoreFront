package com.store.storefront.model;

public class PaymentProcessor {
    public static boolean validate(CardDetails card) {
        //fake processing data
        boolean valid = true;
        if (valid)
            return true;
        return false;
    }
}
